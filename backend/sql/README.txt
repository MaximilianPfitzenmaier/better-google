Create a `databse.txt` file in this folder with 4 rows: host, database name, username, password
For example

localhost (this is the host)
posgres (this is the database name)
posgres (this is the username)
root (this is the password)

So the file should look like: 
localhost 
posgres 
posgres 
root 